import { gql } from '@apollo/client';

const get{{pascalCase nameModule}}Query = gql`
  query get{{pascalCase nameModule}} {
    get{{pascalCase nameModule}} {
      paramOne {
        param
      }
    }
  }
`;

export { get{{pascalCase nameModule}}Query };
